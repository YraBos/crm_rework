MODULE ContractNew;

REQUIRE Contract;

NAMESPACE Contract;

edc 'ЭДО' = DATA BOOLEAN (Contract);

EXTEND FORM contract PROPERTIES (o) edc;

DESIGN contract {  
    number {
        MOVE PROPERTY (edc(o));
    }
}