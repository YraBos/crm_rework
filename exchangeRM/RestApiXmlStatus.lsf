MODULE RestApiXmlStatus;

id = DATA LOCAL STRING(INTEGER);
name = DATA LOCAL STRING(INTEGER);
is_closed = DATA LOCAL STRING(INTEGER);
type = DATA LOCAL STRING();

FORM RestApiIssueStatuses
PROPERTIES() type ATTR

OBJECTS issue_status = INTEGER
PROPERTIES(issue_status) id, name, is_closed
FILTERS imported(issue_status);
